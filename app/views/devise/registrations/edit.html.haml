%h2 Editar Conta

= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put }) do |f|
  = devise_error_messages!

  = f.label :name, "Nome"
  %br
  = f.text_field :name, autofocus: true, placeholder: "Nome completo"
  %br

  = f.label :username, "Nome de usuário"
  %br
  .ps (Não é permitido espaços)
  = f.text_field :username, placeholder: "Nome de usuário"
  %br

  = f.label :age, "Data de nascimento"
  %br
  = f.date_field :age
  %br

  = f.label :email, "Email"
  %br
  = f.email_field :email, placeholder: "nome@examplo.com.br"
  %br

  - if devise_mapping.confirmable? && resource.pending_reconfirmation?
    Currently waiting confirmation for: 
    = resource.unconfirmed_email

  = f.label :password, "Nova senha"
  %br
  (Deixe em branco se você não quiser alterar)
  %br
  = f.password_field :password, :autocomplete => "off"
  %br

  = f.label :password_confirmation, "Confirmação de senha"
  %br
  = f.password_field :password_confirmation
  %br

  = f.label :current_password, "Senha atual"
  %br
  (Nós precisar da sua senha atual para confirmar as alterações na conta)
  %br
  = f.password_field :current_password
  %br

  = f.submit "Atualizar"
  %br
  %br

%h3 Cancelar minha conta

= button_to "Deletar Minha Conta", registration_path(resource_name), data: { confirm: "Você tem certeza?" }, method: :delete
%br
%br

= link_to "Voltar", :back